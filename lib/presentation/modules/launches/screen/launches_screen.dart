import 'package:flutter/material.dart';

import 'package:spacex_launches/presentation/modules/launches/screen/launches_component.dart';

class LaunchesScreen extends StatelessWidget {
  const LaunchesScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) => const LaunchesComponent();
}
