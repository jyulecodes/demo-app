import 'package:flutter/material.dart';

class CountdownValueListTile extends StatelessWidget {
  const CountdownValueListTile({
    Key? key,
    this.value='0',
    this.unit,
  }) : super(key: key);

  final String? value;
  final String? unit;

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
