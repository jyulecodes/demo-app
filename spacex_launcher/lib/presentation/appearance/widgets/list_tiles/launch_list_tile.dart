import 'package:flutter/material.dart';

class LaunchListTile extends StatelessWidget {
  const LaunchListTile({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
