import 'package:flutter/material.dart';

class CountdownValueListTile extends StatelessWidget {
  const CountdownValueListTile({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
